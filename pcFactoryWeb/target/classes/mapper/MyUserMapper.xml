<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ak1ak1.repository.mapper.MyUserMapper">


	<select id="getKlUsers" resultType="com.ak1ak1.entity.MyUser">
		select * from my_user
		<where>
			<if test="query.name != null and  query.name != ''">
				AND CONCAT(user_id,nickname,telPhone,type,created) LIKE '%${query.name}%'
			</if>
		</where>
		ORDER BY created desc
		LIMIT #{query.mStartRow},#{query.limit}
	</select>
	
	<select id="getKlUsersCount" resultType="int">
		select count(id) from my_user 
		<where>
			<if test="query.name != null and  query.name != ''">
				AND CONCAT(user_id,nickname,telPhone,type,created) LIKE '%${query.name}%'
			</if>
		</where>
	</select>
	<!-- 导出 -->
	<select id="exportKlUserInfo" resultType="com.ak1ak1.entity.MyUser">
		select * from my_user
		<where>
			<if test="query.name != null and  query.name != ''">
				AND CONCAT(user_id,nickname,telPhone,type,created) LIKE '%${query.name}%'
			</if>
		</where>
		ORDER BY created desc
	</select>
	
</mapper>